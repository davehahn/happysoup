/* TODO: DELETE ME */
trigger JournalEntry on AcctSeed__Journal_Entry__c (after delete, after insert, after undelete, after update, before delete, before insert, before update) {

//  if(!JournalEntry_dh.disableTrigger)
//  {
//    JournalEntry_dh.triggerHandler
//      (trigger.oldMap,
//       trigger.newMap,
//       trigger.new,
//       trigger.isBefore,
//       trigger.isInsert,
//       trigger.isUpdate,
//       trigger.isDelete);
//  }
}