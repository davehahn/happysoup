trigger gtEmployeeWageRate on Employee_Wage_Rate__c (after delete, after insert, after undelete, after update, before delete, before insert, before update) {

//    if(!gcEmployeeWageRate.disableTrigger)
//    {
//        gcEmployeeWageRate.triggerHandler
//            (trigger.oldMap,
//             trigger.newMap,
//             trigger.new,
//             trigger.isBefore,
//             trigger.isInsert,
//             trigger.isUpdate,
//             trigger.isDelete);
//    }
}